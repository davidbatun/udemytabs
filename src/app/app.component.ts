import { Component } from '@angular/core';
/**Falta Platform, SplashScreen, StatusBar */
@Component({
  selector: 'app-root',
  templateUrl: 'app.component.html',
  styleUrls: ['app.component.scss'],
})

export class AppComponent {
  constructor() {
    /**Falta Platform, SplashScreen, StatusBar */
  }
}

/**Falta Platform, SplashScreen, StatusBar en función initializeApp, despues que esta cargado todo se puede trabajar despues*/
